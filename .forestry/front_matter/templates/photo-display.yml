---
label: Photo Display
hide_body: false
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: category
  type: select
  default: ''
  config:
    required: true
    options: []
    source:
      type: documents
      section: ''
      file: 
      path: 
  label: Catégorie
  description: Catégorie de l'image, doit correspondre à une catégorie énoncée par
    l'artiste
